{
  "name": "studio",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:prod": "NODE_ENV=production node build.mjs",
    "deploy": "node build.mjs && wrangler pages deploy dist --project-name=designcn-studio",
    "deploy:prod": "NODE_ENV=production node build.mjs && wrangler pages deploy dist --project-name=designcn-studio --branch=main"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "mastra": "latest"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
